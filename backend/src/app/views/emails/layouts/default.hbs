<div style="font-family: Roboto, Ubuntu, Arial, Helvetica, sans-serif;
   font-size: 16px;
   line-height: 1.6;
   max-width: 400px;
   min-height: auto;
   display: flex;
   align-items: center;
   flex-direction: column;
   justify-content: center;
   margin: 0 auto;
   text-align: center;
   background-color:rgba(0, 0, 0, 1);
   border-radius: 6px;
">
  {{>header}}
  {{{ body }}}
  {{>footer}}
</div>
